import { NextRequest,NextResponse } from 'next/server'
import { requireAdmin } from '@/lib/auth'
export async function GET(){const a=await requireAdmin();if(!a.ok)return NextResponse.json({error:'Forbidden'},{status:a.status});const {data,error}=await a.supabase.from('profiles').select('id,email,status,role,created_at').order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data})}
export async function PATCH(req:NextRequest){const a=await requireAdmin();if(!a.ok)return NextResponse.json({error:'Forbidden'},{status:a.status});const {id,status}=await req.json();if(!['pending','approved','blocked'].includes(status)||id===a.user.id&&status!=='approved')return NextResponse.json({error:'Invalid change'},{status:400});const {error}=await a.supabase.from('profiles').update({status}).eq('id',id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true})}
