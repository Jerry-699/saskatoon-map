import Link from 'next/link'
import { signup } from '@/app/actions'
export default async function SignupPage({searchParams}:{searchParams:Promise<{message?:string}>}){const p=await searchParams;return <main className="shell"><div className="card"><h1>Create account</h1><p className="muted">New accounts stay pending until the administrator approves them.</p>{p.message?<div className="message">{p.message}</div>:null}<form action={signup}><div><label htmlFor="fullName">Name</label><input id="fullName" name="fullName" required/></div><div><label htmlFor="email">Email</label><input id="email" name="email" type="email" required/></div><div><label htmlFor="password">Password</label><input id="password" name="password" type="password" minLength={10} required/></div><button type="submit">Create account</button></form><p className="muted">Already registered? <Link href="/login">Sign in</Link></p></div></main>}
