import { login, signup } from '@/app/actions'
export default async function Login({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <main className="page"><section className="card"><h1>Saskatoon Block Finder</h1>{q.error&&<div className="error">{q.error}</div>}{q.message&&<div className="ok">{q.message}</div>}<form><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" minLength={6} required/><div className="row"><button formAction={login}>Sign in</button><button formAction={signup}>Sign up</button></div></form><p className="muted"><a href="/forgot-password">Forgot password?</a></p></section></main>}
