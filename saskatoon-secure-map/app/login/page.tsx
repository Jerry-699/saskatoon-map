import Link from 'next/link'
import { login } from '@/app/actions'
export default async function LoginPage({searchParams}:{searchParams:Promise<{message?:string}>}){const p=await searchParams;return <main className="shell"><div className="card"><h1>Saskatoon Block Finder</h1><p className="muted">Sign in to access the private map.</p>{p.message?<div className="message">{p.message}</div>:null}<form action={login}><div><label htmlFor="email">Email</label><input id="email" name="email" type="email" autoComplete="email" required/></div><div><label htmlFor="password">Password</label><input id="password" name="password" type="password" autoComplete="current-password" required/></div><button type="submit">Sign in</button></form><p className="muted">Need an account? <Link href="/signup">Create one</Link></p></div></main>}
