import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { MAP_HTML_BASE64 } from '@/lib/map-html'
export const dynamic='force-dynamic'
export async function GET(request:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.redirect(new URL('/login',request.url));const {data:p}=await s.from('profiles').select('approved,is_admin,blocked').eq('id',user.id).single();if(!p||p.blocked||(!p.approved&&!p.is_admin))return NextResponse.redirect(new URL('/pending',request.url));const html=Buffer.from(MAP_HTML_BASE64,'base64').toString('utf8');return new NextResponse(html,{status:200,headers:{'Content-Type':'text/html; charset=utf-8','Cache-Control':'private, no-store, max-age=0','X-Frame-Options':'DENY','X-Content-Type-Options':'nosniff','Referrer-Policy':'no-referrer','Permissions-Policy':'geolocation=(self)','Content-Security-Policy':["default-src 'self'","script-src 'self' 'unsafe-inline' https://unpkg.com","style-src 'self' 'unsafe-inline' https://unpkg.com","connect-src 'self' https://*.openstreetmap.org https://nominatim.openstreetmap.org https://overpass-api.de https://services.arcgis.com","img-src 'self' data: blob: https://*.openstreetmap.org https://services.arcgis.com","worker-src 'self' blob:","font-src 'self' data:","frame-ancestors 'none'","base-uri 'self'","form-action 'self'"].join('; ')}})}
